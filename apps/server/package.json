{
	"name": "server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "tsc && wrangler deploy --dry-run --outdir=dist",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.2",
		"@cloudflare/workers-types": "^4.20241127.0",
		"typescript": "^5.5.2",
		"vitest": "2.0.5",
		"wrangler": "^3.93.0"
	},
	"dependencies": {
		"@irshadkhan-dev/pandapulse-db": "^0.0.1",
		"@neondatabase/serverless": "^0.10.3",
		"drizzle-orm": "^0.36.1",
		"hono": "^4.6.9"
	}
}
